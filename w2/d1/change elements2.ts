

let nummList: number[] = [1, 2, 3, 8, 5, 6];

function changeElement(oneArray: number[]) {
  return oneArray.map((element: number): number => {
    if (element === 8) {
      return 4;
    } else {
      return element;
    }
  });
};



function myMap (arr, f){
  
};